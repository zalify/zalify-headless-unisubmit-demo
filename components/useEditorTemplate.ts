'use client';
/**
 * Canvas-editor integration stub — this project was scaffolded without
 * the Zalify editor bridge. The storefront always renders its own
 * templates. (Scaffold with `zalify theme create --editor` to get the
 * full z1 canvas-editor wiring instead.)
 */
export function useEditorTemplate(_name: string): null {
  return null;
}
